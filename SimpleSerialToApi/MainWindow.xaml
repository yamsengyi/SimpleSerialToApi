<Window x:Class="SimpleSerialToApi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SimpleSerialToApi"
        Title="Simple Serial To API" Height="400" Width="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Serial Port Configuration -->
        <GroupBox Grid.Row="0" Header="Serial Port" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Label Grid.Column="0" Content="Port:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Text="{Binding SerialPort}" Margin="5,0" VerticalAlignment="Center"/>
                <Button Grid.Column="2" Content="Connect" Command="{Binding ConnectCommand}" Margin="5,0" Padding="10,5"/>
                <Button Grid.Column="3" Content="Disconnect" Command="{Binding DisconnectCommand}" Margin="5,0" Padding="10,5"/>
            </Grid>
        </GroupBox>

        <!-- API Configuration -->
        <GroupBox Grid.Row="1" Header="API Settings" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Label Grid.Column="0" Content="API URL:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Text="{Binding ApiUrl}" Margin="5,0" VerticalAlignment="Center"/>
                <Button Grid.Column="2" Content="Test" Command="{Binding TestApiCommand}" Margin="5,0" Padding="10,5"/>
            </Grid>
        </GroupBox>

        <!-- Timer Control -->
        <GroupBox Grid.Row="2" Header="Timer Control" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Label Grid.Column="0" Content="Send data every 5 seconds" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="Start Timer" Command="{Binding StartTimerCommand}" Margin="5,0" Padding="10,5"/>
                <Button Grid.Column="2" Content="Stop Timer" Command="{Binding StopTimerCommand}" Margin="5,0" Padding="10,5"/>
            </Grid>
        </GroupBox>

        <!-- Status -->
        <GroupBox Grid.Row="3" Header="Status" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Label Grid.Column="0" Content="Status:" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding Status}" Margin="5,0" VerticalAlignment="Center"/>
                <Label Grid.Column="2" Content="Queue:" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="3" Text="{Binding QueueCount}" Margin="5,0" VerticalAlignment="Center" FontWeight="Bold"/>
            </Grid>
        </GroupBox>

        <!-- Indicators -->
        <GroupBox Grid.Row="4" Header="Indicators" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Label Content="Serial Connection" HorizontalAlignment="Center"/>
                    <Ellipse Width="30" Height="30" Fill="Red" 
                             Stroke="Black" StrokeThickness="2"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Label Content="Timer Running" HorizontalAlignment="Center"/>
                    <Ellipse Width="30" Height="30" Fill="Red" 
                             Stroke="Black" StrokeThickness="2"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Footer -->
        <TextBlock Grid.Row="5" Text="Simple Serial To API - Ready" HorizontalAlignment="Center" 
                   Foreground="Gray" FontSize="10" Margin="0,10,0,0"/>
    </Grid>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <local:BoolToColorConverter x:Key="BoolToColorConverter"/>
    </Window.Resources>
</Window>
