<Window x:Class="SimpleSerialToApi.Views.DataMappingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SimpleSerialToApi.Converters"
        Title="Data Mapping and API Configuration" 
        Width="1000" Height="600"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Data Mapping Scenarios and API Configuration" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- Control Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="시나리오 추가" Command="{Binding AddMappingScenarioCommand}" 
                    Margin="0,0,10,0" Padding="15,8" MinWidth="110"/>
            <Button Content="시나리오 복사" Command="{Binding CopyMappingScenarioCommand}" 
                    Margin="0,0,10,0" Padding="15,8" MinWidth="110"/>
            <Button Content="▲ 위로" Command="{Binding MoveUpMappingScenarioCommand}" 
                    Margin="0,0,5,0" Padding="10,8" MinWidth="70"/>
            <Button Content="▼ 아래로" Command="{Binding MoveDownMappingScenarioCommand}" 
                    Margin="0,0,20,0" Padding="10,8" MinWidth="70"/>
            <Button Content="선택 삭제" Command="{Binding DeleteMappingScenarioCommand}" 
                    Margin="0,0,10,0" Padding="15,8" MinWidth="100"/>
            <Button Content="테스트" Command="{Binding TestMappingCommand}" 
                    Margin="0,0,20,0" Padding="15,8" MinWidth="100"/>
            <Button Content="저장" Command="{Binding SaveMappingCommand}" 
                    Margin="0,0,10,0" Padding="15,8" MinWidth="100" Background="LightBlue"/>
        </StackPanel>
        
        <!-- Data Grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <DataGrid Name="MappingDataGrid" 
                      ItemsSource="{Binding MappingScenarios}"
                      SelectedItem="{Binding SelectedMappingScenario}"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="All"
                      HeadersVisibility="All"
                      RowHeight="35"
                      AlternatingRowBackground="LightGray"
                      MinHeight="200"
                      MaxHeight="400">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="활성화" Binding="{Binding IsEnabled}" Width="60"/>
                    <DataGridTextColumn Header="시나리오 이름" Binding="{Binding Name}" Width="120"/>
                    <DataGridComboBoxColumn Header="데이터 소스" SelectedValueBinding="{Binding Source}" 
                                            SelectedValuePath="." Width="100">
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemsSource" Value="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.DataSources}"/>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemsSource" Value="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.DataSources}"/>
                            </Style>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>
                    <DataGridTextColumn Header="식별자" Binding="{Binding Identifier}" Width="100"/>
                    <DataGridTemplateColumn Header="전송값 템플릿" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <TextBox Text="{Binding ValueTemplate, UpdateSourceTrigger=LostFocus}"
                                             BorderThickness="0" VerticalAlignment="Center"
                                             x:Name="ValueTextBox"/>
                                    <TextBlock Text="원본사용" 
                                               Foreground="Gray" FontStyle="Italic"
                                               VerticalAlignment="Center" Margin="3,0"
                                               IsHitTestVisible="False"
                                               Visibility="{Binding ValueTemplate, Converter={StaticResource StringToVisibilityConverter}}"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridComboBoxColumn Header="전송 방식" SelectedValueBinding="{Binding TransmissionType}" 
                                            SelectedValuePath="." Width="90">
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemsSource" Value="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.TransmissionTypes}"/>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemsSource" Value="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.TransmissionTypes}"/>
                            </Style>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>
                    <DataGridTemplateColumn Header="API Method" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.ApiMethods}"
                                          SelectedItem="{Binding ApiMethod, UpdateSourceTrigger=PropertyChanged}"
                                          IsEnabled="{Binding IsApiTransmission}"
                                          BorderThickness="0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="API Url" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding ApiUrl, UpdateSourceTrigger=LostFocus}"
                                         IsEnabled="{Binding IsApiTransmission}"
                                         BorderThickness="0" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="API Endpoint" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding ApiEndpoint, UpdateSourceTrigger=LostFocus}"
                                         IsEnabled="{Binding IsApiTransmission}"
                                         BorderThickness="0" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Headers" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding ApiHeaders, UpdateSourceTrigger=LostFocus}"
                                         IsEnabled="{Binding IsApiTransmission}"
                                         BorderThickness="0" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Content Type" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.ContentTypes}"
                                          SelectedItem="{Binding ContentType, UpdateSourceTrigger=PropertyChanged}"
                                          IsEnabled="{Binding IsApiTransmission}"
                                          BorderThickness="0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Auth Token" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding AuthToken, UpdateSourceTrigger=LostFocus}"
                                         IsEnabled="{Binding IsApiTransmission}"
                                         BorderThickness="0" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
        
        <!-- Footer Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="예약어 목록" Command="{Binding ShowReservedWordsCommand}" 
                    Margin="0,0,10,0" Padding="15,8" MinWidth="100" Background="LightYellow"/>
            <Button Content="확인" IsDefault="True" Command="{Binding ApplyCommand}" 
                    Margin="0,0,10,0" Padding="20,8" MinWidth="80" Background="LightGreen"/>
            <Button Content="닫기" IsCancel="True" Command="{Binding CancelCommand}" 
                    Padding="20,8" MinWidth="80"/>
        </StackPanel>
        
    </Grid>
</Window>
